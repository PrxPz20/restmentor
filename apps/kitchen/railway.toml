[build]
builder = "nixpacks"
buildCommand = "cd ../.. && pnpm install && pnpm --filter @restmentor/kitchen build"

[deploy]
startCommand = "npx serve -s dist -l 3000"
healthcheckPath = "/"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
